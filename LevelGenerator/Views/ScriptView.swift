import SwiftUI

struct ScriptView: View {
    var body: some View {
        Text("Hola Mundo")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(PlatformColor.background)
    }
} 